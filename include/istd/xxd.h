/**
 * \file
 * \brief Function to hexdump memory ranges
 */

#ifndef ISTD_XXD
#define ISTD_XXD

/// \brief Hexdump memory from `begin` (included) to `end` (not included)
void istd_xxd(const void* begin, const void* end);

#endif // ISTD_XXD
